%h1 #{@club.short_name} Registrations

%p= link_to("CSV File", regreport_csv_path(@club.subdomain))

%table
  %tr
    %th Name
    %th Division
    %th Payment Method
    %th Date
    %th ID
    %th Birthdate
    %th Address
    %th City
    %th Phone
    %th Email
    %th Actions
  - divisions = Array.new(@club.divisions.count,0)
  - @registrations.each do |r|
    - divisions[r.division.id.to_i - 1] = divisions[r.division.id.to_i - 1] + 1
    %tr
      %td= "#{r.player.person.first_name} #{r.player.person.last_name}"
      %td= r.division.name
      %td= r.payment_method
      %td= r.created_at
      %td= r.id
      %td= r.player.birthdate
      %td= r.player.person.street1
      %td= r.player.person.city
      %td= r.player.person.phone
      %td= (r.parent_guardian1.nil? ? "" : r.parent_guardian1.email)
      %td= link_to("Delete", delete_reg_path(@club.subdomain, :reg => r.id), :confirm => "Are you sure you want to permanently delete this registration? (ID: #{r.id}, Name: #{r.player.person.first_name} #{r.player.person.last_name})")
      
%hr
- total = 0
- for i in 0..(divisions.size - 1)
  - d = @club.divisions.where(:id => (i + 1)).first
  = d.name
  = ":"
  = divisions[i]
  = link_to(" Mailing List Addresses", mail_list_path(@club.subdomain, :division => d.id))
  %br/
  - total += divisions[i]
Total: 
= total